# Copy to .env.local and fill in secrets before running locally
# Do NOT commit your real API keys.

# Server-only (do NOT expose to client):
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: preferred model name (server-side only). If you want to expose a model selection to the client,
# use NEXT_PUBLIC_MODEL_NAME instead (but never expose API keys).
GEMINI_MODEL=gemini-2.5-flash-lite

# Example: set this in Vercel or your local .env.local
# GEMINI_API_KEY=sk-xxxx-XXXXXXXXXXXXXXXX
# GEMINI_MODEL=gemini-2.5-flash-lite
